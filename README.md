# WordPress Development Environment

To get started:

- Rename .env.example to .env and fill in DB-credentials as needed
- Run docker compose up --build -d
- Build assets inside /wp-content/themes/my-theme
